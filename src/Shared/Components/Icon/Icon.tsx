interface Props {
    iconName: string,
    className?: string,
}

const Icon = (props: Props) => {
    const {iconName, clasName} = props;
    return (
        <div>

        </div>
    )
};

export default Icon;